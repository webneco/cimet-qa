# Secrets - never commit the real key
.env

# Run artifacts and audit logs are generated locally
runs/*.json
runs/*.jsonl

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Dialler recordings and the ASR transcripts made from them
runs/audio/
runs/transcripts/

# ElevenLabs per-line TTS cache (re-created on demand, not billed twice)
data/synth/audio/.cache/
